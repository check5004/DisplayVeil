# Display Veil テスト

## 実施済み

2026-09-06 / Windows / .NET Framework 4.8。

- 警告をエラーとして扱うビルド。
- 初期実装時の `build.ps1 -Test` による自動テスト35件すべて成功。結果の一覧は実行時に標準出力へ表示。検証記録は `artifacts/test-results.txt`。
- 実機の4画面で物理座標と DPI の取得。現在の設定は 2560×1440 が2画面、2560×1080 が2画面、いずれも100%。
- 実機4画面それぞれで、黒幕の矩形が画面全体と厳密に一致すること。
- 黒幕表示・解除・操作バー表示がフォアグラウンドウィンドウを変更しないこと。
- 設定画面を Windows のUI自動化で起動・描画確認。
- 配置図で Windows メイン以外の画面2を選び、対象3画面の表示を確認して開始。
- 再度 exe を起動すると同じインスタンスで全解除・設定復帰すること。
- 設定画面の × でアプリが終了すること。
- 混在解像度・負座標・縦置き・129画面の模擬配置と、鑑賞画面の除外。
- 自動復帰・ピン・ポインター再進入・ドラッグの状態遷移。
- 設定の保存と上書き・古い設定・破損・書込失敗。
- 実際の RegisterHotKey による競合・重複・無効化・解放。
- 黒幕のダブルクリックが最後のマウスアップ後まで解除されないイベント順序。
- 100 / 125 / 150 / 200% の操作バー寸法と描画の確認。

黒幕はアプリ一覧に出ない非アクティブな補助ウィンドウのため、UI自動化ツールから直接クリックできなかった。
マウスでの一連の体験、動画プレイヤー固有の挙動、実際の拡大率変更・ケーブルの抜き差しは次のチェック項目で確認する。
通常の自動テスト中は実際の画面に黒幕を短時間表示し、自動解除する。
GitHub Actionsでは `.\build.ps1 -Test -Headless -Package` で画面を表示しない41件を実行し、実デスクトップを必要とする24件は `SKIP` として報告する。
アイコンの検証はWinForms用の16〜128pxの9サイズの復号・透過・アクセント色を確認する。256pxはExplorer用のPNGフレーム。配布処理テストではWindowsがEXEから取得するアイコンも確認する。
テストログは `artifacts/tests/test-results.txt`。配布フローの検証と公開手順は `RELEASING.md` を参照。

## v1.1.0 更新確認

- 2026-09-06、公開前のローカル検証：Headless 39件成功・9件SKIP、実デスクトップ48件成功・0件SKIP。配布処理の検証29件成功。
- 検証用1.0.0を別設定で実行し、公開済みv1.0.0への実通信で起動時・手動確認の両方が「最新」となることを確認。確認中はボタンが無効になり、完了後に戻る。
- 実機100%表示で設定画面の追加エリアを目視確認。検証用アプリは `artifacts/update-check-test/DisplayVeil.exe`、公開前テストの設定は `artifacts/update-check-test/settings-live/`。
- 自動テスト：正式版と配布ZIPの検証、数値でのバージョン比較、固定GitHubリンク、匿名HTTPリクエスト、404・利用制限・サーバーエラー、24時間制限、自動確認オフ、手動再確認、キャッシュ、同版・旧版、オフライン・タイムアウト、不正レスポンス、重複防止、終了時キャンセル。
- 設定テスト：旧設定で自動確認を有効に初期化し、オフ設定・確認時刻・更新タグを保存・復元する。
- 自動テストはHTTPハンドラーと取得処理を差し替えるため、GitHubへの実通信やブラウザー起動を行わない。
- [ ] 公開後：検証用1.0.0でv1.1.0の通知とリンクを確認する。
- [ ] 公開後：空の検証用設定で起動時の自動検出を確認する。
- [ ] 公開後：配布ZIPの新版で設定引き継ぎと「最新」を確認する。

公開後テストの手順は `RELEASING.md` の「更新通知の公開後テスト」を参照。

## 終了後のプロセス残留の調査（2026-09-07）

- 現行ソース（v1.1.0）では再現せず。本体の変更・バージョン更新・リリースは行っていない。
- 実行ファイルを専用の `--settings-dir` で起動し、設定画面へ終了要求を送信。プロセスが終了することを確認した。
- 別プロセスで動かす終了テストを4件追加。設定画面の終了、最小化後のトレイ終了、鑑賞中のトレイ終了、更新確認が未完了の状態での終了を検証する。
- ウィンドウ・黒幕・画面番号・トレイ・メッセージループの終了と、更新確認のキャンセルを検証したうえで、子プロセスの終了まで待つ。10秒以内に終了しなければテストを失敗させ、テストで起動した子プロセスだけを強制終了する。
- 実デスクトップの `build.ps1 -Test` は52件成功・0件失敗・0件SKIP。
- 残留する場合は、閉じた操作、使用バージョン、タスクマネージャーの「詳細」にあるPIDと「ファイルの場所を開く」で確認できるexeの場所を記録する。同名の別フォルダーのexeかどうかも確認する。

## v1.1.1 全解除後の設定画面復帰（2026-09-07）

- 上記のプロセス残留報告は、黒幕の「すべて解除」を押した後のトレイ待機だったと判明。アプリ終了との見分けがつきやすいよう、黒幕と操作バーの全解除ボタンから設定画面へ戻るように変更した。
- 両ボタンのクリック処理を使うテストを追加。黒幕・操作バーの破棄、設定画面の再表示、鑑賞画面の選択維持、鑑賞の再開、その後の×でのプロセス終了まで検証する。
- 実機4画面の `build.ps1 -Test` は54件成功・0件失敗・0件SKIP。
- トレイ・ショートカットの解除、ロック・スリープによる自動解除は従来の動作を維持する。

## v1.1.2 操作バーのドラッグ移動（2026-09-07）

- 実デスクトップの `build.ps1 -Test` は57件成功・0件失敗・0件SKIP。
- 公開前の `build.ps1 -Test -Headless -Package -ExpectedVersion 1.1.2` は39件成功・0件失敗・18件SKIP。配布処理の検証29件も成功。
- マウスイベントを使った自動テスト3件を追加。実機4画面（負座標を含む）で移動範囲、キャプチャ解除・非表示によるドラッグ終了、右クリックとボタン操作への影響を検証。
- 100 / 125 / 150 / 200%相当の操作バーで、移動量、再表示時の位置維持、フォーカスを取得しないことを検証。実際のマウスによる操作感は下記UX確認の対象。

## 現在バージョンの常時表示（2026-09-09）

- 更新メッセージとは別のラベルに現在バージョンを表示し、新版の検出や通信エラーで消えないようにした。
- ビルド成功。Headlessは39件成功・18件SKIP、対話デスクトップは57件成功・0件失敗・0件SKIP。

## 更新モーダルとリリースノート（2026-09-09）

- ビルド成功。Headlessは41件成功・0件失敗・24件SKIP、対話デスクトップは65件成功・0件失敗・0件SKIP。
- 更新APIの日本語本文・改行・本文未記載、現在版が公開版以上の場合のノート保持を検証。
- モーダルの現在版表示、既知のGitHubリンク、確認中のボタン制御、自動確認設定の保存、通信・ブラウザー起動失敗、本文保持、確認中に閉じて再表示する経路をイベントによる自動テストで検証。
- 設定画面を所有者とするモーダル、表示中の鑑賞開始の抑止、閉じた後の設定復帰、取得中にアプリを終了した場合のモーダル破棄・通信キャンセル・プロセス終了を検証。
- 長いノートのスクロール領域と100 / 125 / 150 / 200%相当のレイアウト、最小サイズを自動検証。実機100%相当の描画画像を目視確認。テストデータによる画像は `artifacts/tests/update-dialog.png`、対話テストの記録は `artifacts/tests/update-dialog-desktop-results.txt`。
- ビルド済み本体の更新クライアントを使い、公開済みv1.1.2の実通信と本文1518文字の取得を確認。ブラウザーの実起動・実際のマウス操作・異なるDPIの画面間移動はこの自動検証に含まない。
- [ ] 設定画面からモーダルを開き、Tabで各操作に移動し、Esc・×・閉じるで設定画面へ戻る。
- [ ] GitHubボタンからブラウザーが開き、案内されたZIPを識別できる。
- [ ] 実際に拡大率が異なる画面間でモーダルを移動し、文字・ボタン・ノートのスクロールを確認する。

## まず5分で確認

- [ ] `DisplayVeil.exe` を開き、「画面番号を表示」で配置と番号を照合する。
- [ ] Windows メイン以外の画面で動画を再生し、その画面を鑑賞用に選ぶ。
- [ ] 開始後、鑑賞画面は表示されたまま、ほかの3画面が端まで黒くなる。
- [ ] 動画の再生・音声が続く。別の画面の黒幕操作で再生が止まらない。
- [ ] 黒幕でマウスを動かすと案内が出て、停止後約2.5秒で消える。
- [ ] 黒い部分のダブルクリックで、その1画面だけが開く。裏のボタンが誤って押されない。
- [ ] 裏のアプリを操作し、マウスを鑑賞画面へ移すと約1.5秒で再び黒くなる。
- [ ] 「固定する」でマウスが離れても開いたままになる。「暗く戻す」で戻せる。
- [ ] Ctrl + Alt + Shift + F11 で全解除できる。

## 追加のUX確認

- [ ] 自動復帰までの1.5秒以内に戻ると、黒くならない。
- [ ] アプリのウィンドウをドラッグして別画面へ運んでいる間は、途中で黒くならない。
- [ ] 開いた画面で文字入力するときに固定を使えば、ポインターを別画面に置いても作業できる。
- [ ] 自動復帰をオフにすると、ピンを使わなくても開いたままになる。
- [ ] 操作バーの左側の状態表示と余白を左ドラッグで移動でき、ボタンは従来どおり使える。
- [ ] 操作バーを画面端へドラッグしても、ボタンが画面外やタスクバーに隠れない。
- [ ] 操作バーを移動しても裏のアプリのフォーカスが変わらず、黒く戻して再び開いても位置が維持される。
- [ ] 案内をオフにしても、右クリックで「すべて解除」を表示できる。
- [ ] 右クリック後にマウスを動かさなくても、約2.5秒はボタンを押せる。
- [ ] F10 の画面開閉キーはポインターのある対象画面だけに効き、鑑賞画面には効かない。
- [ ] F9 を長押ししても連続で開閉しない。
- [ ] 操作バーの「全解除」と黒幕の「すべて解除」で、黒幕と操作バーが消え、設定画面へ戻る。
- [ ] 戻った設定画面から鑑賞を再開でき、×で完全終了できる。
- [ ] トレイの全解除では、設定画面を開かず黒幕を解除する。
- [ ] 設定でキーを変更すると旧キーが解放され、新しいキーが動く。
- [ ] 同じキーを複数の操作に設定すると、重複の表示が出る。
- [ ] すべてのキーを無効化しても、マウスだけで開始・一時操作・全解除・終了できる。
- [ ] exe を再度開くと二重常駐せず、全解除して設定へ戻る。
- [ ] 最小化した設定をトレイから復帰できる。× で完全終了する。

## 画面構成・異常系

映画鑑賞をしていないときに確認してください。黒幕の全解除は F11、右クリック、exe 再起動から行えます。

- [ ] 3440×1440 のウルトラワイドを含む構成で、黒幕の上下左右に隙間がない。
- [ ] 100 / 125 / 150% を画面ごとに混在させても、黒幕・操作バーが欠けない。
- [ ] 設定画面を異なる拡大率のディスプレイへ移しても文字とボタンが使える。
- [ ] 画面を左側・上側・縦向きへ配置しても正しく覆う。
- [ ] 非鑑賞画面を抜くと全解除され、変更後の構成が表示される。
- [ ] 鑑賞画面を抜いた場合も全解除され、別の画面を無断で鑑賞対象にしない。
- [ ] 解像度や拡大率の変更後に、全解除して選択を確認できる。
- [ ] スリープ復帰・ロック解除後に勝手に黒くならない。
- [ ] 1画面構成では開始できず、案内が表示される。
- [ ] 複製表示は個別に覆えないことを確認し、拡張表示に戻す。
- [ ] 普段使う動画プレイヤーのフルスクリーン・HDR・通知の挙動を確認する。

## 再現情報

不具合が出たら「使用した操作」「各画面の解像度・拡大率・配置」「鑑賞画面の番号」「動画プレイヤー名」を記録すると調査しやすい。
アプリの画面番号は Windows の設定画面と一致する保証はない。「画面番号を表示」で確認する。
